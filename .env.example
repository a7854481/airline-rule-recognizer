# 智谱 GLM-4V 视觉识别密钥（必填）
# 去 https://open.bigmodel.cn/ 注册，免费额度可试用 glm-4v-flash
ZHIPU_API_KEY=你的_智谱_API_Key

# 服务端口（可选，默认 3000）
PORT=3000

# 识别模型（可选）
#   glm-4v-plus  —— 准确、支持多图（默认，推荐用于准确率优先）
#   glm-4v       —— 单图视觉理解
#   glm-4v-flash —— 免费，单图，适合低成本
ZHIPU_MODEL=glm-4v-plus
